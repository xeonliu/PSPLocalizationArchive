[project]
name = "psplocalizationarchive"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pycdlib>=1.14.0",
    "pyyaml>=6.0.3",
]
